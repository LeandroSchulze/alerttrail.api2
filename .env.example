# Clave para JWT (cambiá en producción)
SECRET_KEY=changeme-super-secret
ACCESS_TOKEN_EXPIRE_MINUTES=60
DATABASE_URL=sqlite:///./alerttrail.sqlite3
# Coma separada (o * para permitir todo durante pruebas)
CORS_ORIGINS=*
